//! NixFleet CLI library.
//!
//! Ships two subcommands — `status` and `rollout trace <id>` — the
//! minimum operator surface. Later commands land incrementally as CP
//! endpoints gain operator-facing capabilities.
