//! Shared signing fixtures for the verify_* integration tests.
//!
//! The legacy fixture-triple runner that lived here drove the deleted
//! `reconcile()` function — both are gone.

#![allow(dead_code)]

pub mod signing;
