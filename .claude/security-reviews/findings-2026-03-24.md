# Security Review — abstracts33d Fleet (2026-03-24)

> Historical findings from the abstracts33d reference fleet. Preserved as a record.
> Each fleet produces its own findings via `.claude/security-reviews/`.

## High
- [x] bypassPermissions deny list incomplete (dev/home.nix) — resolved: deny list moved to org-level managed policy in core/nixos.nix (non-overridable)
- [x] No explicit firewall config (core/nixos.nix) — resolved: firewall + SSH hardening added

## Medium
- [x] Hashed passwords in nix store world-readable (core/nixos.nix) — resolved: encrypted with agenix
- [x] SPICE without auth on port 5900 (apps.nix) — documented as acceptable for local dev
- [x] SSH TOFU on install (apps.nix) — documented as inherent to provisioning workflow
- [x] OpenSSH not hardened (core/nixos.nix) — resolved: PermitRootLogin, PasswordAuthentication hardened
- [x] trusted-users includes regular user unconditionally (core/nixos.nix) — resolved: gated with !isServer

## Low
- [ ] Typo shashed-password-file (core/nixos.nix) — pending rename in fleet-secrets repo
- [x] Manual JSON in check-config-deps.sh — resolved: uses jq
- [x] allowBroken = true (core/nixos.nix) — resolved: set to false
