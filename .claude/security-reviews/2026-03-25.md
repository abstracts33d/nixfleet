# Security Review — 2026-03-25

**Reviewer:** Claude Opus 4.6 (code-reviewer + security-reviewer agents)
**Scope:** Full repository
**Previous review:** 2026-03-24

## Findings

| # | Severity | File | Finding | Status |
|---|----------|------|---------|--------|
| 1.1 | Medium | `modules/core/nixos.nix:155` | Typo in secrets path: `shashed-password-file` should be `root-hashed-password-file`. Requires rename in nix-secrets repo. | Known (pending nix-secrets rename) |
| 2.1 | Medium | `modules/core/nixos.nix:252-276` | Org deny list pattern matching is glob-based — commands like `bash -c 'rm -rf /'` bypass patterns. Inherent limitation of Claude Code pattern matching. | Known (acceptable limitation) |
| 3.1 | Medium | `modules/apps.nix:459` | SPICE on port 5900 with `disable-ticketing=on`. Implicitly localhost-bound by QEMU defaults but not explicit. | Known (documented as acceptable for local dev) |
| 3.2 | Medium | `modules/apps.nix:251` | SSH `StrictHostKeyChecking=accept-new` in install script (TOFU). Inherent to provisioning. | Known (documented as acceptable) |
| 4.1 | Low | `modules/core/darwin.nix:58-59` | Darwin `trusted-users` not gated by `!isServer` (NixOS was fixed, Darwin was not). Low risk since Darwin hosts are unlikely servers. | New |
| 4.2 | Low | `modules/apps.nix:542` | `test-vm` uses `StrictHostKeyChecking=no` — acceptable for ephemeral test VMs. | New (acceptable) |
| 5.1 | Low | `modules/scopes/dev/home.nix:48` | `skipDangerousModePermissionPrompt = true` with `bypassPermissions`. Intentional for power user workflow, mitigated by org deny list. | Known (intentional) |

## Comparison with Previous (2026-03-24)

### Resolved (7 findings)
| Previous # | Finding | Resolution |
|------------|---------|------------|
| 2.1 (High) | bypassPermissions deny list incomplete | Deny list moved to org-level `/etc/claude-code/settings.json` (non-overridable) |
| 5.1 (High) | No explicit firewall config | `networking.firewall.enable = true` added to core/nixos.nix |
| 1.1 (Medium) | Hashed passwords in nix store | `hashedPasswordFile` now uses agenix (`/run/agenix/*-password`), made unconditional |
| 5.2 (Medium) | OpenSSH not hardened | `PermitRootLogin = "prohibit-password"`, `PasswordAuthentication = false`, `KbdInteractiveAuthentication = false` |
| 8.1 (Medium) | trusted-users unconditional | Gated with `!isServer` on NixOS |
| 2.3 (Low) | Manual JSON in check-config-deps.sh | Uses `jq` now |
| 8.2 (Low) | allowBroken = true | Set to `false` on both NixOS and Darwin |

### Also fixed in this session (not in previous review)
- ISO `PermitRootLogin` hardened from `"yes"` to `"prohibit-password"`
- Darwin agenix identity path fixed from `~/.ssh/` to `~/.keys/` (avoids circular dep)
- `hashedPasswordFile` made unconditional (not gated by isImpermanent)

### New (2 findings)
- 4.1: Darwin trusted-users not gated (Low)
- 4.2: test-vm StrictHostKeyChecking=no (Low, acceptable)

### Unchanged (4 findings)
- 1.1: shashed-password-file typo (pending nix-secrets)
- 2.1: Deny list pattern limitations (acceptable)
- 3.1: SPICE no-auth (documented)
- 3.2: Install SSH TOFU (documented)

## Summary
- **New:** 2 (both Low)
- **Resolved:** 7 (including both High findings)
- **Unchanged:** 4 (all documented/acceptable)
- **Total open:** 7 (0 High, 2 Medium known, 3 Low, 2 acceptable)

## Action Items
1. Rename `shashed-password-file` in nix-secrets repo (Medium, requires external repo change)
2. Gate Darwin `trusted-users` with `!isServer` for consistency (Low)
3. Add explicit `addr=127.0.0.1` to SPICE args (Low, defense-in-depth)
