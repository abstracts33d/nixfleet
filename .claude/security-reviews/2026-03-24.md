# Security Review — 2026-03-24

**Reviewer:** Claude Opus 4.6
**Scope:** Full repository
**Previous review:** None (initial)

## Findings

| # | Severity | File | Finding | Status |
|---|----------|------|---------|--------|
| 2.1 | High | `modules/scopes/dev/home.nix:44` | bypassPermissions + incomplete deny list (rm -r, dd, mkfs, chmod 777 not blocked) | Resolved |
| 5.1 | High | `modules/core/nixos.nix` | No explicit firewall configuration | Resolved |
| 1.1 | Medium | `modules/core/nixos.nix:200,204` | Hashed password files read from nix store (world-readable) | Resolved |
| 3.1 | Medium | `modules/apps.nix:372` | SPICE on port 5900 with auth disabled | Documented (acceptable) |
| 3.3 | Medium | `modules/apps.nix:165` | SSH StrictHostKeyChecking=accept-new on install (TOFU) | Documented (acceptable) |
| 5.2 | Medium | `modules/core/nixos.nix:210` | OpenSSH with default settings, no hardening | Resolved |
| 8.1 | Medium | `modules/core/nixos.nix:31-34` | trusted-users includes regular user unconditionally | Resolved |
| 1.2 | Low | `modules/core/nixos.nix:204` | Typo: shashed-password-file | Resolved |
| 2.3 | Low | `.claude/hooks/check-config-deps.sh:26` | Manual JSON construction (should use jq) | Resolved |
| 8.2 | Low | `modules/core/nixos.nix:21` | allowBroken = true globally | Resolved |

## Good Practices Confirmed
- Hook scripts: no command injection vectors (jq + regex matching only)
- Agenix SSH/GPG keys: mode 600, correct ownership
- Supply chain: all inputs trusted, pinned in lock file, proper follows
- Impermanence btrfs wipe: safe (initrd context, no user-space)
- WiFi bootstrap: secrets properly permissioned, copy-if-absent pattern
- Install key provisioning: short-lived temp dir, chmod 600

## Action Plan

### Batch 1 — High priority (security hardening)
1. Add firewall + SSH hardening to core/nixos.nix
2. Expand claude-code deny list
3. Encrypt hashed passwords with agenix
4. Gate trusted-users with !isServer

### Batch 2 — Medium priority (documentation + cleanup)
5. Document SPICE no-auth as acceptable for local dev
6. Document SSH TOFU as inherent to provisioning
7. Fix typo shashed-password-file
8. Set allowBroken = false

### Batch 3 — Low priority (polish)
9. Use jq in check-config-deps.sh
