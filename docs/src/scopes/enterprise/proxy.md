# Proxy Scope

System-wide HTTP/HTTPS proxy configuration for hosts behind a corporate proxy.

**hostSpec flag:** `useProxy = true`

**Module:** `modules/scopes/enterprise/proxy.nix`

> **Note:** This scope is a stub pending implementation. See the enterprise scopes roadmap.
